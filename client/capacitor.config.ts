import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.myblueprintportfolio.app',
  appName: 'Rohit Kumar Study Hub',
  webDir: 'dist'
};

export default config;
